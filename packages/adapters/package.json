{
  "name": "@english-app/adapters",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "lint": "pnpm exec eslint . --max-warnings=0",
    "test": "vitest run"
  },
  "dependencies": {
    "@english-app/application": "workspace:*",
    "@english-app/domain": "workspace:*",
    "@english-app/observability": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
